CREATE VIEW VIEW_BOOKMARK AS
SELECT BOOKMARK.ID ,BOOKMARK_GROUP.NAME AS GROUP_NAME, RAW_DATA.NAME AS WIFI_NAME,BOOKMARK.DATE
FROM BOOKMARK
LEFT OUTER JOIN RAW_DATA
ON BOOKMARK.MANAGE_ID=RAW_DATA.MANAGE_ID
LEFT OUTER JOIN BOOKMARK_GROUP
ON BOOKMARK.GROUP_ID=BOOKMARK_GROUP.ID;